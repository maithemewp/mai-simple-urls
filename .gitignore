.DS_Store
.git
.env
.npmrc
.cache
.vscode
*.lock
*lock.json
*.zip
LICENSE
README
temp
node_modules
vendor/*
!vendor/autoload.php
!vendor/composer
!vendor/yahnis-elsts
vendor/composer/installers
# installed.php regenerates with the git ref (churns) + is never read at runtime; installed.json is the record.
vendor/composer/installed.php

# deployable-guard's generated pre-commit hook (dev-only; regenerated on install).
.githooks/
